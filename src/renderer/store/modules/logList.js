const state = {
  loggerAllList: {
    aaa: 123
  }
}

const getters = {
  
}

const mutations = {
  common_setLoggerAllList(state, res) {
    state.loggerAllList = res
  }
}

const actions = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
